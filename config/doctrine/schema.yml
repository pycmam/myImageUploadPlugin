detect_relations: true
options:
  type: INNODB
  charset: utf8

Image:
  tableName:   false
  actAs:
    SkipColumns: { skip: [bin] }
  columns:
    size:      { type: integer(8),  notnull: true }
    type:      { type: string(16),  notnull: true }
    bin:       { type: blob,        notnull: true }
  options:
    symfony:
      filter: false
      form:   false

ImageRealty:
  tableName: app_images_realty
  inheritance:
    extends: Image
    type: concrete
  columns:
    realty_id:  { type: integer, notnull: true }
  relations:
    Realty:
      local: realty_id
      foreign: id
      type: one
      foreignType: many
      foreignAlias: Images
      onDelete: CASCADE
